# Django
SECRET_KEY=change-me-use-a-long-random-string
DEBUG=True

# PostgreSQL
DB_NAME=multilingual_rag
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# Gemini LLM (generation only — embeddings are local)
GEMINI_API_KEY=AIza...your-key

# CORS — set to False and list origins explicitly in production
CORS_ALLOW_ALL_ORIGINS=True
# CORS_ALLOWED_ORIGINS=http://localhost:8000,https://yourdomain.com
